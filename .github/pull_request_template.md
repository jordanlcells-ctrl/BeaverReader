## Summary

<!-- What changed and why -->

## Reader assets (check if applicable)

- [ ] N/A — no PDF/EPUB WebView changes
- [ ] Ran `npm run build:readers` after editing `pdfReaderHtml.ts` or reader-related scripts
- [ ] Tested **PDF** reader (canvas + text mode if relevant)
- [ ] Tested **EPUB** reader (Android)
- [ ] Confirmed `readers-build.json` updated / `sha256Prefix` makes sense for changed files
