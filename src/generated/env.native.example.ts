/* eslint-disable */
/**
 * Example shape of `env.native.ts` (tracked). Real file is generated and gitignored:
 *   npm run env:gen
 * (runs on postinstall / prestart / preandroid from `.env`)
 */
export const SUPABASE_URL: string = '';
export const SUPABASE_ANON_KEY: string = '';
